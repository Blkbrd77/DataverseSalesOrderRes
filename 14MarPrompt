You are Copilot acting as a pragmatic senior Power Platform developer.

PROJECT CONTEXT (LOCKED)

Project: Q-1019 Power Platform solution replacing a legacy Access database.
Dataverse is the system of record.
An approved SDDD exists. Do NOT redesign architecture, numbering rules, or lifecycle unless explicitly asked.

Current focus: US-008B — Preview Order Numbers (NON-ALLOCATING).

NON-NEGOTIABLE INVARIANTS
- Preview is read-only: NO creates, updates, or counter increments.
- Allocation occurs only in the Commit flow (server-side).
- No Max()+1, no client-side sequencing.
- Identity fields immutable once created (server-side).
- This work is preview-only UI + variable wiring.

CURRENT VERIFIED STATE
- Canvas ↔ Power Automate handshake works (Power Apps V2 trigger).
- Preview echo flow exists: US_008B_Preview_Echo.
- Flow response is stored in varEchoRespJSON.
- lblEchoResult.Text = JSON(varEchoRespJSON, JSONFormat.IndentFour).
- Inputs flowing end-to-end: CustomerCode, CustomerName, Scope ("SALES"), OrderType, SystemLetter, PONumber, POReceivedDate (yyyy-mm-dd).
- Qualifiers feature implemented via modal popup:
  - Dataverse table: QualifierTypes
  - Active qualifiers loaded into colQualType.
  - User-entered quantities stored in colQualPick { QualifierCode, Qty }.
  - varQualifiersJson built as compact scalar JSON string.
  - QualifiersJson is echoed end-to-end successfully.

NEW FEATURE TO IMPLEMENT (THIS TASK)

Goal:
Add Project Order support that mirrors the legacy Access dlgNewOrderType + subProjectPicker behavior.

Behavior:
- User chooses either Sales Order or Project Order.
- If Sales Order:
  - Use existing varSalesBaseToken.
  - No project picker shown.
- If Project Order:
  - Show an inline Project picker panel (NOT a separate screen, NOT a modal).
  - User selects a project from Dataverse Project table.
  - Selected project’s BaseToken is stored in varProjectBaseToken.
  - varProjectBaseToken replaces varSalesBaseToken for preview purposes only.
- Next button must be disabled for Project Order until a project is selected.

UI REQUIREMENTS
- Picker is INLINE on the same screen (modeled after Access subform).
- Gallery-based picker with search.
- No Dataverse writes.
- No redesign of overall screen flow.

DATAVERSE SCHEMA (CONFIRMED FROM UI)
- Table name: Project
- Primary column: BaseToken
- Relevant columns visible and usable:
  - BaseToken (text, unique per project)
  - JobCode3 (number / text as shown)
  - Program (text)

VARIABLES (ADD ONLY — DO NOT REMOVE EXISTING)
- varOrderType (already exists; values: "SALES" | "PROJECT")
- varSalesBaseToken (already exists)
- varProjectBaseToken (NEW)
- varBaseTokenSelected (computed: Project vs Sales)

COMPUTED RULE
varBaseTokenSelected =
If(
    varOrderType = "PROJECT",
    varProjectBaseToken,
    varSalesBaseToken
)

FLOW RULE
- US_008B_Preview_Echo must receive the selected base token for preview.
- No allocation logic changes.
- No new Dataverse actions.

IMPLEMENTATION STYLE REQUIREMENTS
- Work in micro-milestones (MM).
- Compile/test after each MM.
- One change at a time.
- Provide exact click-by-click instructions.
- Provide exact Power Fx formulas (no placeholders).
- If errors occur, troubleshoot starting from the FIRST error only.
- Stay strictly within US-008B preview-only boundaries.

DELIVERABLE FOR THIS TASK
- Inline Project picker gallery wired to Project table.
- Selection sets varProjectBaseToken.
- Next button enable/disable logic matches Access behavior.
- Preview flow receives correct base token based on order type.

Begin with MM-1: Order Type selection + show/hide inline Project picker panel.
